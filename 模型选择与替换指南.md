# 模型选择与替换指南

## 🔍 模型选择的两个层面

师兄的代码中有**两个层面**的模型选择：

1. **模型架构文件**（LLM_RG4.py vs LLM_RG4_1B_2048.py vs LLM_RG4_1B_4096.py）
2. **LLM模型路径**（Vicuna-7B vs Tiny-Vicuna-1B）

---

## 📝 当前实现方式

### 1. 模型架构文件的选择（硬编码方式）

**位置**: `train.py` 第7行

```python
from models.LLM_RG4 import LLM_RG4
```

**当前状态**: 
- 默认导入 `LLM_RG4.py`
- 如果要使用其他架构，需要**手动修改 import 语句**

### 2. LLM模型路径的选择（参数方式）

**位置**: `config/config.py` 第24行 + 训练脚本

```python
# config.py 默认配置
parser.add_argument('--vicuna_model', default='./hf/vicuna-7b-v1.5', type=str)

# train_stage1.sh 实际使用
vicuna_model="./hf/Tiny-Vicuna-1B"  # 覆盖默认值
```

**当前状态**:
- 通过命令行参数 `--vicuna_model` 指定
- 训练脚本中设置实际路径

---

## 🔧 师兄是如何替换模型的？

### 方法1: 修改 train.py 的 import（架构层面）

如果要使用 `LLM_RG4_1B_2048.py` 或 `LLM_RG4_1B_4096.py`，需要修改 `train.py`:

**原始代码** (`train.py` 第7行):
```python
from models.LLM_RG4 import LLM_RG4
```

**替换为 2048 版本**:
```python
from models.LLM_RG4_1B_2048 import LLM_RG4
```

**替换为 4096 版本**:
```python
from models.LLM_RG4_1B_4096 import LLM_RG4
```

**注意**: 三个文件中的类名都是 `LLM_RG4`，所以只需要改 import 路径！

### 方法2: 修改训练脚本的 vicuna_model 参数（LLM层面）

**在训练脚本中修改** (`scripts/train_stage1.sh`):

```bash
# 使用 Vicuna-7B
vicuna_model="./hf/vicuna-7b-v1.5"

# 使用 Tiny-Vicuna-1B
vicuna_model="./hf/Tiny-Vicuna-1B"
```

---

## 📊 完整的替换组合

### 组合1: 7B架构 + Vicuna-7B（原作者配置）

**train.py**:
```python
from models.LLM_RG4 import LLM_RG4
```

**train_stage1.sh**:
```bash
vicuna_model="./hf/vicuna-7b-v1.5"
```

**结果**: 使用完整的7B模型，最佳性能

---

### 组合2: 1B架构(2048) + Tiny-Vicuna-1B（轻量级）

**train.py**:
```python
from models.LLM_RG4_1B_2048 import LLM_RG4
```

**train_stage1.sh**:
```bash
vicuna_model="./hf/Tiny-Vicuna-1B"
```

**结果**: 轻量级配置，资源占用最小

---

### 组合3: 1B架构(4096) + Tiny-Vicuna-1B（标准版）

**train.py**:
```python
from models.LLM_RG4_1B_4096 import LLM_RG4
```

**train_stage1.sh**:
```bash
vicuna_model="./hf/Tiny-Vicuna-1B"
```

**结果**: 1B模型的标准配置，平衡性能和资源

---

## 🎯 师兄的实际使用方式

从训练脚本 `train_stage1.sh` 可以看到：

```bash
#vicuna_model="./hf/vicuna-7b-v1.5"  # 被注释掉
vicuna_model="./hf/Tiny-Vicuna-1B"   # 实际使用
```

**推测师兄的做法**:
1. 修改了 `train.py` 的 import，从 `LLM_RG4` 改为 `LLM_RG4_1B_4096`（或2048）
2. 在训练脚本中指定 `Tiny-Vicuna-1B` 路径
3. 这样实现了从7B到1B的完整替换

---

## 💡 更优雅的实现方式（改进建议）

当前实现需要手动修改 `train.py`，不够灵活。可以改进为：

### 方案1: 添加命令行参数

**修改 `config/config.py`**:
```python
parser.add_argument('--model_arch', type=str, default='LLM_RG4', 
                    choices=['LLM_RG4', 'LLM_RG4_1B_2048', 'LLM_RG4_1B_4096'],
                    help='Model architecture to use')
```

**修改 `train.py`**:
```python
# 动态导入
model_arch = args.model_arch
if model_arch == 'LLM_RG4':
    from models.LLM_RG4 import LLM_RG4
elif model_arch == 'LLM_RG4_1B_2048':
    from models.LLM_RG4_1B_2048 import LLM_RG4
elif model_arch == 'LLM_RG4_1B_4096':
    from models.LLM_RG4_1B_4096 import LLM_RG4
else:
    raise ValueError(f"Unknown model architecture: {model_arch}")
```

**使用方式**:
```bash
python train.py --model_arch LLM_RG4_1B_4096 --vicuna_model ./hf/Tiny-Vicuna-1B
```

### 方案2: 使用 importlib 动态导入

**修改 `train.py`**:
```python
import importlib

def load_model_class(model_arch):
    module = importlib.import_module(f'models.{model_arch}')
    return module.LLM_RG4

# 使用
LLM_RG4 = load_model_class(args.model_arch)
model = LLM_RG4(args)
```

---

## 🔍 代码中的关键点

### 1. 为什么三个文件类名相同？

三个文件都定义了 `class LLM_RG4`，这样设计的好处：
- ✅ 接口统一，可以无缝替换
- ✅ 不需要修改其他代码
- ✅ 只需要改 import 语句

### 2. 模型路径如何传递？

```python
# 所有模型文件都使用相同的参数
self.llama_model = LlamaForCausalLM.from_pretrained(
    args.vicuna_model,  # 通过参数传入
    torch_dtype=torch.bfloat16,
)
```

**关键**: `args.vicuna_model` 可以是任何兼容的LLM模型路径！

### 3. Embedding维度如何适配？

```python
# 动态获取embedding维度
self.embed_dim = self.llama_model.config.hidden_size
print(f'LLM embedding dimension: {self.embed_dim}')
```

**关键**: 代码会自动适配不同模型的embedding维度！

---

## 📋 实际替换步骤

### 从7B切换到1B（完整流程）

**步骤1**: 修改 `train.py`
```python
# 原来
from models.LLM_RG4 import LLM_RG4

# 改为
from models.LLM_RG4_1B_4096 import LLM_RG4
```

**步骤2**: 修改训练脚本 `train_stage1.sh`
```bash
# 原来
vicuna_model="./hf/vicuna-7b-v1.5"

# 改为
vicuna_model="./hf/Tiny-Vicuna-1B"
```

**步骤3**: 确保模型文件存在
```bash
# 检查模型路径
ls ./hf/Tiny-Vicuna-1B/
```

**步骤4**: 运行训练
```bash
bash scripts/train_stage1.sh
```

---

## 🎓 设计模式分析

### 当前设计：策略模式（Strategy Pattern）

```
train.py (Context)
    ↓
LLM_RG4 (Strategy Interface)
    ↓
├── LLM_RG4.py (Concrete Strategy 1: 7B)
├── LLM_RG4_1B_2048.py (Concrete Strategy 2: 1B轻量)
└── LLM_RG4_1B_4096.py (Concrete Strategy 3: 1B标准)
```

**优点**:
- 接口统一
- 易于扩展
- 可以灵活切换

**缺点**:
- 需要手动修改代码
- 不够自动化

---

## 🔧 快速切换脚本

可以创建一个切换脚本：

```bash
#!/bin/bash
# switch_model.sh

MODEL_ARCH=$1  # LLM_RG4, LLM_RG4_1B_2048, or LLM_RG4_1B_4096

# 备份原文件
cp train.py train.py.bak

# 替换import语句
sed -i "s/from models\\.LLM_RG4.*import LLM_RG4/from models.${MODEL_ARCH} import LLM_RG4/" train.py

echo "Switched to ${MODEL_ARCH}"
```

**使用**:
```bash
bash switch_model.sh LLM_RG4_1B_4096
```

---

## 📝 总结

### 模型选择的两个维度

| 维度 | 控制方式 | 位置 | 灵活性 |
|------|---------|------|--------|
| **架构文件** | 修改 import | `train.py` 第7行 | ⚠️ 需要改代码 |
| **LLM模型** | 命令行参数 | `--vicuna_model` | ✅ 灵活 |

### 师兄的替换方式

1. **修改 `train.py`**: 改变 import 语句选择架构
2. **修改训练脚本**: 改变 `vicuna_model` 变量选择LLM
3. **组合使用**: 架构和LLM可以独立选择

### 最佳实践

1. **开发阶段**: 使用1B版本快速迭代
2. **正式训练**: 根据资源选择合适配置
3. **性能对比**: 同时训练多个配置进行对比

---

**提示**: 虽然需要手动修改代码，但设计很清晰，三个架构文件接口统一，替换起来很简单！

