# BN5212-final-VLMR 快速参考指南

## 🎯 核心概念速查

### 四种场景（RG4）

| 场景 | 含义 | 输入 | 代码标识 |
|------|------|------|----------|
| **sn** | Single-view, No prior report | 单张图像 | `dataset_id='sn'` |
| **sw** | Single-view, With prior report | 单张图像 + 历史报告 | `dataset_id='sw'` |
| **mn** | Multi-view, No prior report | 正位图像 + 侧位图像 | `dataset_id='mn'` |
| **mw** | Multi-view, With prior report | 正位图像 + 侧位图像 + 历史报告 | `dataset_id='mw'` |

### 两阶段训练

| 阶段 | 训练内容 | 冻结模块 | 可训练模块 |
|------|----------|----------|------------|
| **Stage 1** | 视觉-文本对齐 | LLM、视觉编码器、文本编码器 | APPA模块 |
| **Stage 2** | LLM微调 + 多模态融合 | 视觉编码器、文本编码器 | LoRA、Lateral分支、Text分支、IIT融合 |

### 模型组件

```
┌─────────────┐
│ rad-dino    │ → 视觉编码器（冻结）
└─────────────┘
       ↓
┌─────────────┐
│   APPA      │ → 对齐和投影模块（Stage 1训练）
└─────────────┘
       ↓
┌─────────────┐
│  Lateral    │ → 侧位图像分支（Stage 2训练）
└─────────────┘
       ↓
┌─────────────┐
│    Text     │ → 历史报告分支（Stage 2训练）
└─────────────┘
       ↓
┌─────────────┐
│    IIT      │ → 图像-图像-文本融合（Stage 2训练）
└─────────────┘
       ↓
┌─────────────┐
│   Vicuna    │ → 大语言模型（Stage 1冻结，Stage 2 LoRA微调）
└─────────────┘
```

## 📝 关键代码位置

### 1. 模型初始化
- **文件**: `models/LLM_RG4.py`
- **方法**: `__init__()`
- **关键行**: 
  - 30-34: 加载视觉编码器
  - 35-39: 加载文本编码器
  - 42-56: 加载LLM
  - 67-83: Stage 1 APPA模块
  - 84-131: Stage 2 Lateral/Text/IIT模块

### 2. 图像编码
- **文件**: `models/LLM_RG4.py`
- **方法**: `encode_APPA_img()`
- **关键行**: 228-247
- **流程**: 视觉编码 → 交叉注意力 → 投影层 → LLM embedding

### 3. 前向传播
- **文件**: `models/LLM_RG4.py`
- **方法**: `forward()`
- **关键行**: 407-538
- **流程**: 根据`dataset_id`选择处理方式 → IIT融合 → LLM生成

### 4. 损失计算
- **文件**: `models/LLM_RG4.py`
- **方法**: `forward()`
- **关键行**: 498-538
- **公式**: `total_loss = average_loss + 0.75 * key_loss`

### 5. 验证评估
- **文件**: `models/LLM_RG4.py`
- **方法**: `validation_step()`, `calculate_metric()`
- **关键行**: 569-763
- **指标**: BLEU, ROUGE, CIDEr, METEOR, CheXbert F1

## 🔧 常用参数配置

### Stage 1 训练
```bash
--stage_class 1
--llm_use_lora False
--visual_token_number 128
--batch_size 40
--learning_rate 3e-4
--max_epochs 2
```

### Stage 2 训练
```bash
--stage_class 2
--llm_use_lora True
--llm_r 32
--llm_alpha 64
--visual_delta_file ./save/.../checkpoint_epoch1_...pth
--batch_size 32
```

### 测试
```bash
--test
--test_mode sn  # 或 sw, mn, mw
--delta_file ./save/.../checkpoint_epoch1_...pth
```

## 🎓 学习重点

### 1. APPA模块设计
- **位置**: `encode_APPA_img()`
- **关键点**: 
  - 可学习查询token
  - 交叉注意力对齐
  - 渐进式投影（768→2048→4096→embed_dim）

### 2. IIT融合机制
- **位置**: `forward()`中的各场景处理
- **关键点**:
  - 使用zero padding统一维度
  - 三个分支拼接后线性投影
  - 支持灵活的输入组合

### 3. 句子加权损失
- **位置**: `forward()`中的损失计算
- **关键点**:
  - Token级别损失 + 句子级别加权
  - 重要句子权重更高
  - λ=0.75的平衡系数

### 4. 多场景统一处理
- **位置**: `forward()`, `validation_step()`
- **关键点**:
  - 通过`dataset_id`自动选择处理方式
  - 统一的IIT融合接口
  - 灵活的zero padding策略

## 🐛 常见问题

### Q1: 为什么Stage 1要冻结LLM？
**A**: 先学习视觉-文本对齐，避免LLM参数干扰对齐过程。

### Q2: IIT融合为什么要用zero padding？
**A**: 统一不同场景的输入维度，使融合层可以处理任意输入组合。

### Q3: 句子加权损失的作用？
**A**: 让模型更关注重要的医学发现，提高关键信息的生成质量。

### Q4: 为什么Stage 2要混合所有场景的数据？
**A**: 让模型学习处理不同输入组合，提高泛化能力。

## 📊 评估指标说明

| 指标 | 含义 | 范围 | 越高越好 |
|------|------|------|----------|
| **BLEU-4** | 4-gram精确匹配 | 0-1 | ✅ |
| **CIDEr** | 共识导向评估 | 0-∞ | ✅ |
| **ROUGE-L** | 最长公共子序列 | 0-1 | ✅ |
| **METEOR** | 考虑同义词的匹配 | 0-1 | ✅ |
| **CheXbert F1** | 医学标签F1分数 | 0-1 | ✅ |

## 🚀 快速开始

### 1. 准备数据
```bash
# 下载MIMIC-CXR图像
# 下载标注文件到 dataset/annotation/
# 下载预训练模型到 hf/
```

### 2. Stage 1 训练
```bash
bash scripts/train_stage1.sh
```

### 3. Stage 2 训练
```bash
# 修改 train_stage2.sh 中的 visual_delta_file
bash scripts/train_stage2.sh
```

### 4. 测试
```bash
# 修改 test.sh 中的 test_mode 和 delta_file
bash scripts/test.sh
```

## 📚 相关文件

- **详细讲解**: `代码详细讲解.md`
- **README**: `README.md`
- **主模型**: `models/LLM_RG4.py`
- **配置文件**: `config/config.py`

---

**提示**: 建议先阅读`代码详细讲解.md`了解整体架构，再使用本指南作为快速参考。










