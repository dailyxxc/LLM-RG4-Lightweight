# Tiny-Vicuna-1B 下载问题诊断和解决方案

## ✅ 模型确认存在

根据 Hugging Face 网站验证，模型确实存在：
- **模型ID**: `Jiayi-Pan/Tiny-Vicuna-1B`
- **模型页面**: https://huggingface.co/Jiayi-Pan/Tiny-Vicuna-1B
- **模型大小**: 1B 参数
- **格式**: Safetensors

---

## 🔍 问题分析

### 遇到的错误

从之前的错误日志可以看到：

```
ConnectionResetError: [Errno 104] Connection reset by peer
urllib3.exceptions.ProtocolError: ('Connection aborted.', ConnectionResetError(104, 'Connection reset by peer'))
```

### 可能的原因

1. **网络连接问题** ⚠️
   - 连接在 SSL 握手阶段被重置
   - 可能是防火墙/代理拦截
   - 网络不稳定或中断

2. **地理位置限制** 🌍
   - 某些地区可能无法直接访问 Hugging Face
   - 需要配置代理或镜像

3. **SSL/TLS 证书问题** 🔐
   - 证书验证失败
   - 加密协议不兼容

4. **服务器临时问题** 🚫
   - Hugging Face 服务器暂时不可用
   - 需要稍后重试

---

## 🛠️ 解决方案

### 方案1：使用 Hugging Face 镜像（如果在国内，强烈推荐）

如果在国内，可以使用镜像站点：

```bash
cd /mnt/nvme_disk/user8_data/BN5212-final-VLMR
conda activate llm_rg4

# 设置镜像环境变量
export HF_ENDPOINT=https://hf-mirror.com

# 使用 Python 下载
python scripts/download_tiny_vicuna_python.py
```

或者修改 Python 脚本使用镜像。

### 方案2：配置代理

如果您的网络需要通过代理访问：

```bash
# 设置代理环境变量
export HTTP_PROXY=http://proxy.example.com:8080
export HTTPS_PROXY=http://proxy.example.com:8080

# 然后运行下载脚本
cd /mnt/nvme_disk/user8_data/BN5212-final-VLMR
conda activate llm_rg4
python scripts/download_tiny_vicuna_python.py
```

### 方案3：使用 Git Clone（可能更稳定）

有时候 Git 连接比 HTTP 更稳定：

```bash
cd /mnt/nvme_disk/user8_data/BN5212-final-VLMR/hf

# 安装 Git LFS（如果未安装）
conda install -c conda-forge git-lfs -y
git lfs install

# 删除空目录（如果存在）
rm -rf Tiny-Vicuna-1B

# 克隆模型
git clone https://huggingface.co/Jiayi-Pan/Tiny-Vicuna-1B
```

### 方案4：使用符号链接（最快，无需网络）

如果网络问题无法解决，可以直接使用符号链接：

```bash
cd /mnt/nvme_disk/user8_data/BN5212-final-VLMR
bash scripts/download_tiny_vicuna.sh
# 选择 2（创建符号链接）
```

**优点**：
- ⚡ 几秒钟完成
- ✅ 不需要网络
- ✅ TinyLlama 和 Tiny-Vicuna 高度兼容

---

## 🔧 改进的 Python 下载脚本（带错误处理和重试）

让我创建一个更健壮的下载脚本，包含：
- 错误重试机制
- 更详细的错误信息
- 镜像支持













